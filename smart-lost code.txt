#include <stdio.h>
#include <string.h>

#define MAX 100
struct FoundItem
{
    int id;
    char name[50];
    char category[50];
    char description[100];
    char location[100];
    char finder[50];
};

// Arrays

struct LostItem lost[MAX];
struct FoundItem found[MAX];

int lostCount = 0;
int foundCount = 0;
void addLostItem();
void showLostItems();
void searchLostItem();

void addFoundItem();
void showFoundItems();
void matchItems();

void saveData();
void loadData();
